/**
 * Leistungsmessung fuer E18 (GDD 16 Abschnitt 15). Kein Teil des Spiels - ein Werkzeug.
 *
 * Gemessen wird, was der Plan verlangt: gleichzeitige Gegner, gleichzeitige Geschosse und
 * die Zeit je Simulationstakt. Gezeichnet wird dabei nicht; die Frage ist, ob die
 * Simulation das Budget haelt.
 */

import { createInitialState } from '../../../../../E:/spiele/Desktop/The Tower/src/app/state.ts'

